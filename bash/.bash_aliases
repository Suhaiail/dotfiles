
# Short git status
alias gs="git status -sb"

# Pull with rebase
alias gp="git pull --rebase"

# Quick git diff
alias gd="git diff"

# Colored grep output
alias grep="grep --color=auto"

# Create dirs recursively
alias mk="mkdir -p"
